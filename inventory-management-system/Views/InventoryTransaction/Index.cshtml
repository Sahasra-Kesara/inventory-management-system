@model IEnumerable<inventory_management_system.Models.InventoryTransaction>

<h2>Inventory Transactions</h2>

<div class="table-responsive">
    <table class="table">
        <thead>
            <tr>
                <th>Transaction ID</th>
                <th>Item</th>
                <th>Supplier</th>
                <th>Type</th>
                <th>Quantity</th>
                <th>Date</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var transaction in Model)
            {
                <tr>
                    <td>@transaction.TransactionId</td>
                    <td>@transaction.Item?.ItemName</td>
                    <td>@transaction.Supplier?.SupplierName</td>
                    <td>@transaction.TransactionType</td>
                    <td>@transaction.Quantity</td>
                    <td>@transaction.TransactionDate.ToShortDateString()</td>
                </tr>
            }
        </tbody>
    </table>
</div>

@* <a asp-action="AddStock" class="btn btn-primary">Add Stock</a> *@
@* <a asp-action="RemoveStock" class="btn btn-danger">Remove Stock</a> *@
